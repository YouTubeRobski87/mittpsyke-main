---
layout: layouts/base.njk
title: Logga in
permalink: /login/
disableFooter: true
---

{% block headExtra %}
  <style>
    .login-shell {
      max-width: 520px;
      margin: 0 auto;
      padding: var(--space-2xl) var(--space-lg);
      background: rgba(255, 255, 255, 0.02);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 18px;
      box-shadow: 0 24px 60px rgba(0, 0, 0, 0.25);
      backdrop-filter: blur(8px);
    }
    .login-title {
      text-align: center;
      margin-bottom: var(--space-sm);
      font-size: 1.75rem;
    }
    .login-lede {
      text-align: center;
      margin-bottom: var(--space-xl);
      color: var(--muted);
      line-height: 1.6;
    }
    .login-actions {
      display: grid;
      gap: var(--space-sm);
      margin-top: var(--space-lg);
    }
    .login-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      padding: 12px 16px;
      border-radius: 12px;
      border: 1px solid rgba(255, 255, 255, 0.12);
      background: rgba(255, 255, 255, 0.06);
      color: var(--on-surface, #f7f7ff);
      text-decoration: none;
      font-weight: 600;
      transition: transform 120ms ease, background 120ms ease, border-color 120ms ease;
    }
    .login-btn:hover {
      transform: translateY(-1px);
      background: rgba(255, 255, 255, 0.08);
      border-color: rgba(255, 255, 255, 0.16);
    }
    .login-btn.secondary {
      background: transparent;
      border-color: rgba(255, 255, 255, 0.10);
      color: var(--muted);
    }
    .login-note {
      margin-top: var(--space-lg);
      color: var(--muted);
      text-align: center;
      line-height: 1.5;
    }
    .login-footnote {
      margin-top: var(--space-sm);
      text-align: center;
      color: var(--muted);
      font-size: 0.95rem;
    }
  </style>
{% endblock %}

<main class="container" style="padding: var(--space-3xl) 0;">
  <div class="login-shell">
    <h1 class="login-title">Spara dina samtal (valfritt)</h1>
    <p class="login-lede">
      Om du vill kan du logga in för att fortsätta AI-stödet över tid.
      Du kan också använda AI-stödet utan konto.
    </p>

    <div class="login-actions">
      <a class="login-btn" href="#" data-login-action="google">
        <span>Fortsätt med Google</span>
      </a>
      <a class="login-btn secondary" href="#" data-login-action="magic">
        <span>Få länk via e-post (magic link)</span>
      </a>
    </div>

    <p class="login-note">
      Inloggning gör att AI-stödet kan minnas vad ni pratat om.
      Du bestämmer alltid själv vad som sparas.
    </p>

    <p class="login-footnote">Kom ihåg: AI-stödet fungerar även utan inloggning.</p>
  </div>
</main>

{% block scripts %}
  <script>
    (function () {
      var client = window.mpSupabase;
      if (!client) return;

      function redirectIfLogged() {
        client.auth.getSession().then(function (res) {
          if (res && res.data && res.data.session) {
            window.location.href = '/ai/';
          }
        });
      }

      redirectIfLogged();

      client.auth.onAuthStateChange(function (event) {
        if (event === 'SIGNED_IN') {
          window.location.href = '/ai/';
        }
      });

      function handleMagicLink() {
        var email = window.prompt('Ange din e-post för att få en inloggningslänk:');
        if (!email) return;
        client.auth.signInWithOtp({ email: email }).then(function (res) {
          if (res.error) {
            alert('Kunde inte skicka länk: ' + res.error.message);
            return;
          }
          alert('Kolla din e-post för en magisk länk.');
        });
      }

      function handleGoogle() {
        client.auth.signInWithOAuth({
          provider: 'google',
          options: { redirectTo: window.location.origin + '/ai/' }
        }).then(function (res) {
          if (res.error) alert('Kunde inte logga in: ' + res.error.message);
        });
      }

      var googleBtn = document.querySelector('[data-login-action="google"]');
      var magicBtn = document.querySelector('[data-login-action="magic"]');
      if (googleBtn) googleBtn.addEventListener('click', function (e) { e.preventDefault(); handleGoogle(); });
      if (magicBtn) magicBtn.addEventListener('click', function (e) { e.preventDefault(); handleMagicLink(); });
    })();
  </script>
{% endblock %}
