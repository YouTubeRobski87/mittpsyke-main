---
layout: layouts/base.njk
title: Ditt st&ouml;d - p&aring; dina villkor
---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <h1>{{ site.title }}</h1>
      <p class="tagline">{{ site.tagline }}</p>
      <div class="hero-actions" style="margin-top: var(--space-xl); display: flex; justify-content: center;">
        <a
          href="/ai/"
          style="
            display: inline-block;
            padding: 12px 20px;
            background: var(--accent);
            color: var(--on-accent);
            border-radius: var(--radius-full);
            font-weight: 600;
            text-decoration: none;
            box-shadow: var(--shadow);
          "
        >
          Starta AI-st&ouml;d
        </a>
      </div>
    </div>
  </div>
</section>

<section class="container" style="padding-bottom: var(--space-3xl);">
  <h2 style="text-align: center; margin-bottom: var(--space-xl);">Vad k&auml;mpar du med?</h2>
  <p style="text-align: center; margin: -0.5rem 0 var(--space-md); color: var(--muted);">
    V&auml;lj det som st&auml;mmer b&auml;st just nu.
  </p>

  <div class="portal-grid">
    {% for portal in collections.portaler %}
    <a href="{{ baseUrl }}/ai/?context={{ portal.url }}" class="portal-card">
      <span class="portal-card-icon">{% icon portal.data.icon, "portal-card-icon-glyph" %}</span>
      <h3>{{ portal.data.title }}</h3>
      <p class="portal-card-description">{{ portal.data.description | truncateWords(15) }}</p>
      <span class="portal-card-link">
        <span>L&auml;s mer</span>
        {% icon "symbol-arrow-right", "portal-card-link-icon" %}
      </span>
    </a>
    {% endfor %}
  </div>
</section>

<section class="container urgent-lines">
  <h2 style="text-align: center; margin-bottom: var(--space-xl);">Akuta linjer</h2>
  <div class="hotline-grid">
    {% include "partials/hotline-cards.njk" %}
  </div>
  <p style="text-align: center; margin-top: var(--space-md); color: var(--muted); font-size: 0.95rem;">
    Vid akut fara: ring 112. Beh&ouml;ver du prata direkt: Sj&auml;lvmordslinjen 90101.
  </p>
</section>

{# Citat #}
{% set randomQuote = quotes | random %}
<section class="quote-section">
  <div class="container">
    <blockquote class="quote">
      <p>"{{ randomQuote.text }}"</p>
      <cite>&ndash; {{ randomQuote.author }}</cite>
    </blockquote>
  </div>
</section>

<section class="container" style="padding: var(--space-3xl) 0;">
  <h2 style="text-align: center; margin-bottom: var(--space-xl);">Alla st&ouml;dlinjer</h2>

  <div class="support-grid">
    {% for line in supportlines | selectattr("urgent") %}
    {% include "partials/support-card.njk" %}
    {% endfor %}
  </div>

  <p style="text-align: center; margin-top: var(--space-xl);">
    <a href="/stodlinjer/" style="font-weight: 600;">Se alla {{ supportlines | length }} st&ouml;dlinjer &rarr;</a>
  </p>
</section>
